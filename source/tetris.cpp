#define DEBUG2
#define DEBUG32

#include "std.h"

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.precision(10);

	int n;
	cin >> n;
	DEB cout << "n: " << n << endl;

	return 0;
}
